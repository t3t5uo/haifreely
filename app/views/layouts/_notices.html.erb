<% flash.each do |key, value| %>
          <br>
          <div class="<%= flash_class(key) %>">
                <%= value %>
          </div>
          <!--TODO<div class="<%= flash_class(key) %> alert-dismissible">
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            <strong>Heads up!</strong> This <a href="#" class="alert-link"><%= value %></a>
          </div>-->
    <% end %>
